# 黑马记账 — 项目文档

## 产品概述
- **产品名称**：黑马记账
- **产品定位**：个人日常记账 Web 应用
- **运行方式**：浏览器打开，数据存储在本地文件，无需联网
- **货币单位**：人民币（¥ / 元）

## 核心功能
1. **记一笔**：记录花销金额、选择分类、填写日期和备注
2. **两级分类**：一级大类 → 二级小类，先选大类再选小类
3. **历史记录**：按时间倒序展示所有花销记录，支持按分类筛选
4. **统计总览**：按月查看各分类支出合计
5. **数据持久化**：所有数据存为 JSON 文件，关闭再打开数据不丢失

## 分类体系

| 一级分类 | 二级小类 |
|----------|----------|
| 🍽️ 餐饮 | 早餐、午餐、晚餐、零食、饮料、聚餐 |
| 🚗 交通 | 公交、地铁、出租车、加油、停车、火车飞机 |
| 🛍️ 购物 | 衣物、电子产品、日用品、化妆品、书籍 |
| 🏠 居住 | 房租、水电、物业、维修、家具 |
| 🎮 娱乐 | 电影、游戏、旅游、运动、KTV |
| 🏥 医疗 | 门诊、药品、体检、住院 |
| 📚 教育 | 学费、培训、书本、考试 |
| 💰 金融 | 保险、理财、贷款、手续费 |
| 📌 其他 | 礼品、捐赠、宠物、其他杂项 |

## 技术栈

| 层面 | 技术 | 说明 |
|------|------|------|
| 桌面框架 | Electron 43 | 将 Web 技术打包为 Windows/Mac 桌面应用 |
| 前端框架 | React 18 + TypeScript | 用户界面 |
| 后端服务 | Express 5 + tsx | API 服务，处理数据读写 |
| 数据存储 | JSON 文件（data/ledger.json） | 零依赖，存在项目文件夹内 |
| 样式方案 | TailwindCSS | 界面样式 |
| 构建工具 | Vite | 前端开发与打包 |
| 目标平台 | Windows / macOS 桌面 | Electron 提供桌面窗口 |

## 项目结构

```
ledger/
├── electron/
│   └── main.cjs          # Electron 主进程入口
├── server/
│   ├── index.ts          # Express 后端入口（API + 静态文件托管）
│   └── store.ts          # JSON 数据读写模块
├── src/renderer/
│   ├── index.html        # HTML 入口
│   ├── main.tsx          # React 入口
│   ├── App.tsx           # 主界面（记一笔 / 明细 / 统计）
│   ├── types.ts          # 类型定义 + API 封装
│   └── index.css         # 全局样式
├── data/
│   └── ledger.json       # 记账数据文件
├── package.json
├── vite.config.ts
├── tsconfig.json
├── tailwind.config.mjs
└── CLAUDE.md
```

## 启动方式

```bash
npm install        # 安装依赖（仅首次）
npm run dev        # 启动开发模式
# 浏览器打开 http://localhost:5173
```

## 约法三章（项目协作规则）

> **这是本项目最重要的规则，贯穿整个开发过程。**

1. **技术决策由用户拍板**：因用户不懂编程技术，所有关于技术的选择（框架、库、工具、架构）必须做到：
   - 列出 ≥2 个可行方案
   - 用通俗语言解释每个方案"是什么、好在哪、差在哪"
   - 给出一个推荐方案并说明理由
   - 由用户在了解后做最终决定

2. **非技术决策同样提供选项**：UI 布局、配色方案、交互方式等，均需提供 2-3 个选项让用户选择。

3. **不自作主张**：禁止在未经用户确认的情况下自行选定技术方案或做出重大设计决定。

## 变更记录

| 日期 | 变更内容 | 原因 |
|------|----------|------|
| 2026-08-04 | 选用 Electron + React + TypeScript 方案 | 用户从 3 个方案中选定 |
| 2026-08-04 | 数据存储：better-sqlite3 → sql.js | 编译失败 |
| 2026-08-04 | 数据存储：sql.js → JSON 文件 | ESM/CJS 兼容问题 |
| 2026-08-04 | 尝试 Electron 33/28/42 | 镜像版二进制功能异常，官方源下载超时 |
| 2026-08-04 | 尝试切换到 Tauri | Rust 工具链下载超时 |
| 2026-08-04 | 临时切换为 Web 应用 | Electron 下载受阻，先以浏览器方式运行 |
| 2026-08-04 | **最终方案：Electron + React + TypeScript + Express** | Electron 由用户手动安装成功；修复 ELECTRON_RUN_AS_NODE 环境变量污染问题后正常运行 |
